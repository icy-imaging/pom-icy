<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.bioimageanalysis.icy</groupId>
    <artifactId>parent-pom-base</artifactId>
    <version>1.0.1</version>

    <packaging>pom</packaging>

    <name>Parent POM base</name>
    <description>Common parent POM for all Icy related projects</description>
    <url>http://icy.bioimageanalysis.org/</url>
    <inceptionYear>2020</inceptionYear>

    <organization>
        <name>Institut Pasteur</name>
        <url>https://www.pasteur.fr/</url>
    </organization>

    <licenses>
        <license>
            <name>GNU GPLv3</name>
            <url>https://www.gnu.org/licenses/gpl-3.0.en.html</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>stef</id>
            <name>Stéphane Dallongeville</name>
            <url>http://icy.bioimageanalysis.org/author/stef/</url>
            <roles>
                <role>founder</role>
                <role>lead</role>
                <role>developer</role>
                <role>debugger</role>
                <role>reviewer</role>
                <role>support</role>
                <role>maintainer</role>
            </roles>
        </developer>
        <developer>
            <id>jtinevez</id>
            <name>Jean-Yves Tinevez</name>
            <url>http://icy.bioimageanalysis.org/author/tinevez/</url>
            <roles>
                <role>lead</role>
                <role>reviewer</role>
                <role>support</role>
            </roles>
        </developer>
        <developer>
            <id>dgonzalez</id>
            <name>Daniel Felipe González Obando</name>
            <url>http://icy.bioimageanalysis.org/author/danyfel80/</url>
            <roles>
                <role>developer</role>
                <role>debugger</role>
                <role>maintainer</role>
                <role>support</role>
            </roles>
        </developer>
        <developer>
            <id>atournay</id>
            <name>Amandine Tournay</name>
            <roles>
                <role>developer</role>
                <role>maintainer</role>
            </roles>
        </developer>
    </developers>

    <mailingLists>
        <mailingList>
            <name>Icy</name>
            <post>icy-all@pasteur.fr</post>
        </mailingList>
    </mailingLists>

    <issueManagement>
        <system>GitLab</system>
        <url>https://gitlab.pasteur.fr/xx/issues</url>
    </issueManagement>

    <properties>
        <!-- Project Information -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jdk.version>[1.8,)</jdk.version>

        <!-- Compile information -->
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>

        <!-- Maven Plugins version -->
        <maven-compiler-plugin.version>3.2</maven-compiler-plugin.version>
        <maven-resources-plugin.version>3.1.0</maven-resources-plugin.version>
        <maven-source-plugin.version>3.2.1</maven-source-plugin.version>
        <maven-javadoc-plugin.version>3.2.0</maven-javadoc-plugin.version>
        <maven-dependency-plugin.version>3.1.1</maven-dependency-plugin.version>
        <maven-jar-plugin.version>3.2.0</maven-jar-plugin.version>
        <maven-enforcer-plugin.version>3.0.0-M3</maven-enforcer-plugin.version>
        <maven-help-plugin.version>3.2.0</maven-help-plugin.version>

        <!-- Maven plugin properties -->
        <maven-source-plugin.includePom>true</maven-source-plugin.includePom>
        <maven-jar-plugin.includeMavenDescriptor>false</maven-jar-plugin.includeMavenDescriptor>
    </properties>

    <!-- POM profiles -->
    <profiles>
        <profile>
            <id>compile-project</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <!-- Properties for this profile -->
            <build>
                <defaultGoal>clean install</defaultGoal>
            </build>
        </profile>
    </profiles>

    <build>
        <finalName>${project.artifactId}-${project.version}</finalName>

        <!-- List of pre-configured Maven plugins -->
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven-resources-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>${maven-dependency-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                    <configuration>
                        <outputDirectory>${outputJar}</outputDirectory>
                        <archive>
                            <addMavenDescriptor>${maven-jar-plugin.includeMavenDescriptor}</addMavenDescriptor>
                        </archive>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                    <configuration>
                        <outputDirectory>${outputJar}</outputDirectory>
                        <includePom>${maven-source-plugin.includePom}</includePom>
                    </configuration>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                    <configuration>
                        <jarOutputDirectory>${outputJar}</jarOutputDirectory>
                    </configuration>
                    <executions>
                        <execution>
                            <id>attach-javadoc</id>
                            <goals>
                                <goal>jar</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-help-plugin</artifactId>
                    <version>${maven-help-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>show-current-profile</id>
                            <phase>compile</phase>
                            <goals>
                                <goal>active-profiles</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${maven-enforcer-plugin.version}</version>
                    <dependencies>
                        <dependency>
                            <groupId>org.codehaus.mojo</groupId>
                            <artifactId>extra-enforcer-rules</artifactId>
                            <version>1.3</version>
                        </dependency>
                    </dependencies>
                    <executions>
                        <execution>
                            <id>enforce-rules</id>
                            <goals>
                                <goal>enforce</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
                        <rules>
                            <!-- Check if there is duplicate classes -->
                            <!--<banDuplicateClasses> <message>No Duplicate 
                                Classes Allowed! - For duplicate transitive dependencies, add dependency 
                                exclusions. - For duplications between direct dependencies, resolve or add 
                                ignored classes to this rule's configuration.</message> <ignoreClasses> <ignoreClass>org.pushingpixels.*</ignoreClass> 
                                <ignoreClass>org.jdesktop.*</ignoreClass> <ignoreClass>org.slf4j.*</ignoreClass> 
                                </ignoreClasses> <findAllDuplicates>true</findAllDuplicates> </banDuplicateClasses> -->

                            <!-- Prevent circular dependencies -->
                            <!--<banCircularDependencies/> -->

                            <!-- Prevent usage of snapshots dependencies 
                                in the project -->
                            <requireReleaseDeps>
                                <message>Your dependency cannot be a
                                    snapshot</message>
                            </requireReleaseDeps>
                        </rules>
                        <fail>true</fail>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <!-- Required Maven plugins -->
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <!-- Link to third-party Maven repositories -->
    <repositories>
        <repository>
            <id>ome-repo</id>
            <name>OME repository</name>
            <url>https://artifacts.openmicroscopy.org/artifactory/ome.releases/</url>
        </repository>
        <repository>
            <id>geomajas</id>
            <name>Geomajas</name>
            <url>http://maven.geomajas.org/</url>
        </repository>
        <repository>
            <id>icy</id>
            <name>Icy's Nexus</name>
            <url>https://icy-nexus.pasteur.fr/repository/Icy/</url>
        </repository>
    </repositories>

    <!-- Icy Maven repositories for POM deployment -->
    <distributionManagement>
        <snapshotRepository>
            <id>icy-dev</id>
            <name>Icy Dev's Nexus</name>
            <url>https://icy-nexus-dev.pasteur.cloud/repository/icy-core/</url>
        </snapshotRepository>
        <repository>
            <id>icy-prod</id>
            <name>Icy's Nexus</name>
            <url>https://icy-nexus.pasteur.fr/repository/icy-core/</url>
        </repository>
    </distributionManagement>

</project>